import React, { Component } from 'react';
import "./Square.css";

export default function Square (props) {React.ReactElement} {
    return (
      <button className="square" 
      onClick={()=>this.props.onClick()}>{this.props.value}</button>
    )
}
